<!--index.wxml-->
<view class="container">
  <head bind:navEmit="navEmit"></head>
  <!-- 左侧菜单 -->
  <menu wx:if="{{navCurrent === 'menu'}}"></menu>
  <add-menu wx:if="{{navCurrent === 'addMenu'}}"></add-menu>
</view>
